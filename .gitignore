# Dependencies
node_modules/

# Env (never commit secrets)
.env
.env.local
.env.*.local

# Firebase service account key (contains private key)
**/firebase-key.json
santokki/firebase-key.json

# Google Cloud / GCP service account keys (never commit)
tidy-drive*.json

# Python
__pycache__/
*.pyc
*.pyo

# Build / logs
dist/
*.log
.DS_Store
